{
    "name": "lan-media-browser",
    "version": "1.0.0",
    "private": true,
    "description": "LAN-accessible local media file explorer with tagging and search",
    "scripts": {
        "install:all": "npm install && powershell -NoProfile -Command \"Push-Location server; npm install; Pop-Location; Push-Location client; npm install; Pop-Location\"",
        "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
        "build": "npm run build --prefix client",
        "start": "npm run start --prefix server"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}